/**
 * App.jsx
 * Phase: React Migration v1 — Foundation
 *
 * Root React component. Currently just renders AppShell.
 * Future phases will add context providers, global state, etc.
 */
import AppShell from './components/AppShell';

export default function App() {
  return <AppShell />;
}
